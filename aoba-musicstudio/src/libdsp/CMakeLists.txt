FILE(GLOB_RECURSE LIBDSP_SRC src *.cpp)
FILE(GLOB_RECURSE LIBDSP_INCLUDE include *.hpp)

SET(CMAKE_CXX_STANDARD 11)

ADD_LIBRARY(libdsp ${LIBDSP_SRC} ${LIBDSP_INCLUDE})
TARGET_INCLUDE_DIRECTORIES(libdsp PUBLIC include)
